# Agent Memory

This file is loaded by devtool-copilot as persistent memory at startup.
Add notes, preferences, and project context you want the AI to always remember.

## Project conventions

<!-- e.g. "Always use tabs for indentation", "Prefer async/await over callbacks" -->

## Key facts

<!-- e.g. "The main entry point is src/index.ts", "Tests live in __tests__/" -->
